#!/bin/bash
# Automates GNU Stow symlinking for every package in directory

# Install stow if not already installed
if [[ ! $(dpkg-query -s stow | grep "Status: install ok installed") ]]; then
  apt install stow
fi

# Stow every package in directory
for package in */; do
  stow $package
done
