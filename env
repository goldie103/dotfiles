# vim: set ft=sh
export PATH="/bin:$HOME/bin:/usr/local/sbin:/usr/local/bin:/usr/bin:/usr/bin/site_perl:/usr/bin/vendor_perl:/usr/bin/core_perl"

export XDG_CONFIG_HOME="${XDG_CONFIG_HOME:-$HOME/.config}"
export XDG_CACHE_HOME="${XDG_CACHE_HOME:-$HOME/.cache}"

export ZDOTDIR="$XDG_CONFIG_HOME/zsh"
export ZSH=$ZDOTDIR/oh-my-zsh
export HISTFILE="$XDG_CONFIG_HOME/zsh/history"
#export XINITRC="$XDG_CONFIG_HOME/X11/xinitrc"
export VIMPERATOR_RUNTIME=$XDG_CONFIG_HOME/vimperator
export VIMPERATOR_INIT=":source $VIMPERATOR_RUNTIME/init.vimp"
#export PENTADACTYL_INIT=":source $XDG_CONFIG_HOME/pentadactyl/init.penta"
#export VIMDOTDIR="$XDG_CONFIG_HOME/vim"
#export VIMINIT='let $MYVIMRC "$VIMDOTDIR/init.vim" | source $MYVIMRC'
export ALTERNATE_EDITOR=""
export EDITOR='nvim'
export VISUAL='emacsclient -c -a emacs'

export UDIR=/media/User && [[ ! -d $UDIR ]] && export UDIR=$HOME
export DOT=$UDIR/dev/dot && [[ ! -d $DOT ]] && export DOT=$UDIR/dotfiles
